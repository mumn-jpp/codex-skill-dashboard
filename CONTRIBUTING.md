# Contributing

Use Node.js 22 or newer. Create focused changes, add a failing test before production code, run `npm test`, and never test trash behavior against a real Codex home. Do not commit personal paths, annotations, logs, credentials, or generated package archives.
